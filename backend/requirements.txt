# OpenExec backend API (FastAPI on EC2, per plan at
# .claude/plans/curious-orbiting-shore.md)

fastapi>=0.115
uvicorn[standard]>=0.32
boto3>=1.35

# Test-only
pytest>=7.4.0
httpx>=0.27.0
moto[dynamodb]>=5.0
moto[server]>=5.0  # moto_server — a local dynamodb-local-style stand-in for manual/live testing
