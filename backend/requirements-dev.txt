# Test-only dependencies. Kept out of backend/requirements.txt so the runtime
# image (backend/Dockerfile) does not ship pytest or httpx.
#
# No database stand-in here: the suite runs against a real Postgres (see
# tests/conftest.py). moto had no Postgres equivalent to replace it with.
-r requirements.txt

pytest>=7.4.0
httpx>=0.27.0
