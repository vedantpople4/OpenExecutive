# Test-only dependencies. Kept out of backend/requirements.txt so the runtime
# image (backend/Dockerfile) does not ship moto, pytest, or httpx.
-r requirements.txt

pytest>=7.4.0
httpx>=0.27.0
moto[dynamodb]>=5.0
moto[server]>=5.0  # moto_server — a local dynamodb-local-style stand-in for manual/live testing
