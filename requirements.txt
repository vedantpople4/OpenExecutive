# OpenExec Dependencies

# Core — the CLI entry point (openexec/cli.py) imports both of these, so a
# `pip install -e .` without them yields an install that cannot be imported.
typer>=0.9.0
rich>=13.0.0

# Configuration

# Text processing (for report generation)

# AI/LLM Integration (OpenAI-compatible format)
requests>=2.31.0
json5>=0.9.0

# Web search (free, no API key)
ddgs>=9.0.0

# Development
pytest>=7.4.0
black>=23.0.0
ruff>=0.1.0
